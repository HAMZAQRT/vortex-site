- name: Prepare Android Project
  run: |
    npm ci || npm install   # يجرب ci أولاً، وإذا فشل يستخدم install
    mkdir -p www
    cp index.html www/
    
    rm -rf android
    npx cap add android
    npx cap sync android
