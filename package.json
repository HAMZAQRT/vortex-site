{
  "name": "vortex-network",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Building web assets...'",
    "ionic:build": "npm run build",
    "ionic:cap:build:android": "npx cap add android && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "6.0.0",
    "@capacitor/cli": "6.0.0",
    "@capacitor/core": "6.0.0"
  }
}
